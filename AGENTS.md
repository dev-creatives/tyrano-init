## Codexでどのモデルを利用するか

`codex` を利用する際、原則としてプランには `gpt-5.6-terra` を、実装や単純作業には `gpt-5.6-luna` を用います。
したがって、プランと実装は同時に行わないでください。実装に移る前に一度立ち止まってユーザーの承認を得てください。
ただし、単純で明確な指示の場合は指示があった時点で承認も得ているとみなしてよいです。

実装フェーズでLunaに切り替えるかTerraのままにするかは次の基準に従うことにします。

| 実装フェーズ | 判断 |
|---|---|
| 数分で終わりそう、1〜2ファイル | **Terraのまま** |
| 小さなバグ修正、数十行程度 | **Terraのまま** |
| 計画後にかなりコードを書く | **Lunaへ切替** |
| 多数ファイルを機械的に変更 | **Lunaへ切替** |
| テスト修正を何度も繰り返す | **Lunaへ切替** |
| 実装中にも設計判断が頻発しそう | **Terraのまま** |

## Git操作

作業単位でコミットしてください。
後で、どういう目的でどのような変更がなされたのかが追跡できるようにする、という意図です。
